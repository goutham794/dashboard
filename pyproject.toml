[project]
name = "dashboard"
version = "0.1.0"
description = "Personal dashboard experiments, starting with a YouTube daily curator."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "textual>=8.2.7",
]

[project.scripts]
yt-curator = "dashboard.youtube_curator.cli:main"
